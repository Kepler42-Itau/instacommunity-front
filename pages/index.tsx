import type { NextPage } from "next";
import React, { useState } from "react";
import NavBar from "../components/NavBar";

const Home: NextPage = () => {
  const userId = 1;
  return (
    <NavBar />
  );
};

export default Home;
